
export const currentPokemonSelector = (state: any) => state.pokemonDetailed.currentPokemon;

export const currentPokemonNameSelector = (state: any) => state.pokemonDetailed.name;

export const isLoadingSelector = (state: any) => state.pokemonDetailed.isLoading;
